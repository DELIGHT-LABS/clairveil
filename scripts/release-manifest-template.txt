Clairveil release handoff pack
==============================

version: @VERSION@
commit: @COMMIT@
generated_at_utc: @GENERATED_AT_UTC@
source_repo: github.com/DELIGHT-LABS/clairveil

Contents:
- root license, notice, changelog, contribution, and security files
- English and Korean current-knowledge pairs and the documentation index
- proto/clairveil/privacy/v1
- getting-started, architecture, circuit, BatchJoinSplit16x32, CLI, testing, and localnet documents
- downstream Cosmos integration, JS/web wallet, and reference payroll/control-plane implementation contracts
- common/deposit prover HTTP contracts and the remote prover operations profile
- operations, Merkle restore, threat model, and maintainer documents
- language-neutral prover HTTP API schema and separate wallet-facing JSON schemas
- prover HTTP, deposit prover, NoteV1, batch, reservation, relay, and wallet conformance fixtures
- JS audit disclosure key, fixture validator, prover HTTP client, and reference payroll examples
- prover Dockerfile and compose sample
- release pack generation and verification scripts

Validation before handoff:
- make docs-check
- make release-check (includes the static one-proof contract gate and a live legacy multi-message regression, but not the live one-proof gate)
- RUN_LOCALNET=1 make privacy-batch-joinsplit-localnet before claiming live one-proof integration readiness
- make privacy-bulk-readiness-check from the source checkout for extra synthetic/legacy bulk diagnostics, not one-proof capacity evidence
- RUN_PROVER_SCALE=1 PROVERD_URLS=url1,url2 make privacy-bulk-readiness-check before making prover-pool scale claims
- make release-pack
- make release-pack-verify

Notes:
- This pack is a handoff contract bundle, not a full source distribution.
- For a publishable release, create the final release commit and its annotated
  exact-SemVer tag after release-check, then run release-pack and
  release-pack-verify from that tagged commit.
- A snapshot-<full-commit-sha> version is for packaging CI/internal checks and
  must not be published as a release.
- scripts/release-pack-paths.txt selects pack paths and
  scripts/release-pack-required-files.txt defines exact required membership.
- Ignored tmpdocs archives are not release inputs.
- A one-proof 16x32 production-capacity claim additionally requires a tag/commit-bound actual-workload artifact recording proof/sec, tx/sec, item/sec, RSS/CPU, shape distribution, retry/replan/manual-review outcomes, and circuit/artifact/environment identity. Synthetic and legacy results cannot be relabeled as that evidence.
- Downstream production apps must still own EVM/policy/precompile integration,
  audit private key custody, wallet storage encryption, artifact signing, and
  remote prover deployment policy.
