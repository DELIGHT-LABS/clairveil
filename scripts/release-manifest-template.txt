Clairveil release handoff pack
==============================

version: @VERSION@
commit: @COMMIT@
generated_at_utc: @GENERATED_AT_UTC@
source_repo: github.com/DELIGHT-LABS/clairveil

Contents:
- root license, notice, changelog, contribution, and security files
- English and Korean durable knowledge pairs, documentation index, and plan status index
- proto/clairveil/privacy/v1
- getting-started, architecture, client, JS/web wallet, and scan optimization handoff documents
- current BatchJoinSplit16x32, bulk product handoff, reference payroll, and readiness materials
- language-neutral prover HTTP API schema and separate wallet-facing JSON schemas
- general prover HTTP, deposit prover, and wallet conformance fixtures
- JS audit disclosure key, fixture validator, prover HTTP client, and reference payroll examples; browser DApp excluded
- prover Dockerfile and compose sample
- release pack generation and verification scripts
- release, circuit, CLI, testing, operations, downstream integration, client, JS SDK, scan optimization, prover, Merkle restore, threat model, security, and reference app documents
- NoteV1 and BatchJoinSplit16x32 foundation contracts and independent golden fixtures
- BatchJoinSplit16x32 independent publication validation, benchmark, localnet, finding, and residual-risk reports
- current tracked plan ledgers and source-repo readiness commands; superseded plans excluded
- release versioning policy and release note templates

Validation before handoff:
- make docs-check
- make release-check
- make privacy-bulk-readiness-check from the source checkout when doing extra bulk-readiness rehearsal
- RUN_PROVER_SCALE=1 PROVERD_URLS=url1,url2 make privacy-bulk-readiness-check before making prover-pool scale claims
- make release-pack
- make release-pack-verify

Notes:
- This pack is a handoff contract bundle, not a full source distribution.
- For a publishable release, create the final release commit and its annotated
  exact-SemVer tag after release-check, then run release-pack and
  release-pack-verify from that tagged commit.
- A snapshot-<full-commit-sha> version is for packaging CI/internal checks and
  must not be published as a release.
- scripts/release-pack-paths.txt selects pack paths and
  scripts/release-pack-required-files.txt defines exact required membership.
- Ignored tmpdocs archives and superseded phase-1 plans are not release inputs.
- Downstream production apps must still own EVM/policy/precompile integration,
  audit private key custody, wallet storage encryption, artifact signing, and
  remote prover deployment policy.
